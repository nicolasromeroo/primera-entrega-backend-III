// import Task from '../models/Task.js';  // Supongamos que tienes un modelo de "Task"

// // Función para obtener las tareas de un usuario
// export const getUserTasks = async (userId) => {
//   try {
//     // Buscar las tareas en la base de datos donde el campo userId coincida
//     const tasks = await Task.find({ userId });
//     return tasks;  // Retorna las tareas
//   } catch (err) {
//     throw new Error('Error al obtener las tareas: ' + err.message);
//   }
// };
